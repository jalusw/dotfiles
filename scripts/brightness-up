#!/usr/bin/env bash
set -euo pipefail
# wrapper for unified brightness script - keeps 10% step for compatibility
# allows absolute 0 via -n0 and recovers via exponential curve
exec "$(dirname "$0")/brightness" up 10%


