{
  "layer": "top",
  "position": "top",
  "modules-left": ["hyprland/workspaces"],
  "modules-center": ["hyprland/window"],
  "modules-right": ["tray", "pulseaudio", "memory", "disk#root", "temperature", "battery",  "clock"],

  "hyprland/window": {
    "max-length": 20
  },

  "hyprland/workspaces": {
      "format": "{name}",
      "all-outputs": true,
      "active-only": false,
      "show-special": true,
  },

  "clock": {
    "format": "{:%a %b %d  %H:%M}",
    "format-alt": "{:%Y-%m-%d  %H:%M}",
    "tooltip-format": "<big>{:%B %Y}</big>\n<tt>{calendar}</tt>",
    "calendar": {
      "mode": "month",
      "on-scroll": 1
    }
  },

  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-icons": ["", "", ""],
    "on-click": "pavucontrol"
  },

  "memory": {
    "interval": 5,
    "format": " {percentage}%"
  },

  "disk#root": {
    "path": "/",
    "interval": 30,
    "format": " {percentage_used}%"
  },

  "temperature": {
    "interval": 5,
    "critical-threshold": 80,
    "format": "{temperatureC}°C {icon}",
    "format-icons": ["", "", ""]
  },

  "battery": {
    "format": "{capacity}% {icon}",
    "format-icons": ["", "", "", "", ""]
  },

  "tray": {
      "spacing": 12
    }
}

