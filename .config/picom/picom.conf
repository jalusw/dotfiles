# Backend (use "glx" for better performance)
backend = "glx";

# Exclude some windows from effects (e.g., rofi, notifications)
opacity-rule = [
  "100:class_g = 'Rofi'",
  "100:class_g = 'Polybar'",
  "80:class_g = 'kitty'"
];
